/*
package com.ziyun.basic.controller;

import com.ziyun.basic.entity.EduStatus;
import com.ziyun.basic.entity.OwnOrgStudent;
import com.ziyun.basic.server.EduStatusServer;
import com.ziyun.basic.server.StudentServer;
import org.junit.Test;
import org.junit.runner.RunWith;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.boot.test.context.SpringBootTest;
import org.springframework.test.context.junit4.SpringJUnit4ClassRunner;

*/
/**
 * @Descrition:
 * @Created by Administrator on 2018/6/7.
 *//*

@SpringBootTest(classes = StudentApplication.class)
@RunWith(SpringJUnit4ClassRunner.class)
public class BasicControllerTest {

    @Autowired
    private StudentServer studentServer;
    @Autowired
    private EduStatusServer eduStatusServer;

    @Test
    public void selectByPrimaryKey() {
        OwnOrgStudent ownOrgStudent = studentServer.selectByPrimaryKey("134575865");
        System.out.println(ownOrgStudent.toString());
    }

    @Test
    public void selectByOutid() {
        EduStatus eduStatus = eduStatusServer.selectByOutid("134575865");
        System.out.println(eduStatus.toString());
    }
}
*/
